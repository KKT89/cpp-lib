# verify 方針

- verify コードは `verify/<judge>/` に置く。
- verify コードは `<problem>.cpp` で命名する。
- verify ドキュメントは `docsrc/verify/<judge>/` に置く。
- verify ドキュメントは `<problem>.md` で命名する。
- 1ライブラリに対して複数ジャッジの verify を並べることがある（1:多）。
